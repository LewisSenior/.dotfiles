[engine]
# Stage layer temp data inside graphRoot (on /home) instead of the default
# /var/tmp. This host's /var is small; committing large image layers there
# fails mid-write ("io: read/write on closed pipe").
image_copy_tmp_dir = "storage"
