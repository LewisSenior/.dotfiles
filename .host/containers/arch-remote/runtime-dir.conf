# No logind session is opened in this container (systemd's userdb cannot
# resolve the keep-id user in the nested userns), so nothing creates
# /run/user/1000 on its own. waypipe and Wayland clients expect it to exist,
# and sshd_config points XDG_RUNTIME_DIR at it.
d /run/user/1000 0700 lewis lewis -
